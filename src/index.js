import React from 'react'; //импорт ядра реакта
import ReactDOM from 'react-dom/client'; //импорт дома
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));//импорт app в дом root
root.render(
  <App />
);